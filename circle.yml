machine:
  services:
    - docker
  hosts:
    s2.stridespace.net: 54.202.81.91

general:
  branches:
    only:
      - standalone
      - circle

dependencies:
  pre:
    - curl -s https://testspace-client.s3.amazonaws.com/testspace-linux-dev.tgz | tar -zxvf- -C $HOME/bin
    - testspace config url http://s2.stridespace.net
    - testspace -v
    
test:
  override:
    - echo test override
    
  post:
    - ./incremental.sh 
